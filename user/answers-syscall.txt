Which function called syscall?
Ans:usertrap() in kernel/trap.c:68 called syscall()

Q2: What is the value of p->trapframe->a7 and what does it represent?
Ans:p->trapframe->a7 = 0x87f56000

Q3: What was the previous mode the CPU was in?
Ans: sstatus = 0x200000022

Q4: What assembly instruction is the kernel panicking at?
ans. lw a5,0(a0) at address 0x80001bfe.

Q5: Which register corresponds to num?
ans. num corresponds to register a5.

Q6: Why does the kernel crash?
and. The kernel tries to dereference address 0x0, which is not mapped in kernel address space (causing a load access fault, scause=0xd).

Q7: What is the name and PID of the process running when the kernel panicked?
ans. Process name: init, PID: 1
